// Compatibility alias for existing brand-specific helper signatures. New UI code
// owns a CameraSelectionStore directly and does not create this legacy name.
typealias CameraData = CameraSelectionStore
